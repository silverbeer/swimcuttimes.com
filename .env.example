# Environment configuration template
#
# For environment-specific settings, use:
# - .env.local.example  -> .env.local (local development)
# - .env.dev.example    -> .env.dev (dev cloud)
# - .env.prod.example   -> .env.prod (production cloud)
#
# Then switch using: ./scripts/use-local.sh, use-dev.sh, or use-prod.sh

ENVIRONMENT=local

# API Keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Supabase
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=your-anon-key-here

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=console
