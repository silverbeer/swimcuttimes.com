# swimcuttimes Backend

FastAPI backend and CLI for swim cut times tracking.

## Development

```bash
uv sync                    # Install dependencies
uv run python -m cli       # Run CLI
uv run pytest              # Run tests
```

See [project documentation](../docs/) for more details.
